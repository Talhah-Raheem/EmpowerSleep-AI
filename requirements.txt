# EmpowerSleep RAG Chatbot - Dependencies
# =========================================
# Install with: pip install -r requirements.txt

# Vector store and embeddings
faiss-cpu>=1.7.4          # Facebook AI Similarity Search (CPU version)

# LLM integration
openai>=1.0.0             # For GPT API calls

# PDF extraction
PyMuPDF>=1.23.0           # PDF text extraction (imported as fitz)

# Backend API
fastapi>=0.109.0          # FastAPI framework
uvicorn>=0.27.0           # ASGI server
pydantic>=2.0.0           # Data validation

# Web scraping
requests>=2.31.0          # HTTP requests for blog scraping
beautifulsoup4>=4.12.0    # HTML parsing for blog scraping

# Database
supabase>=2.0.0           # Supabase client for feedback storage

# Utilities
python-dotenv>=1.0.0      # Environment variable management
numpy                     # Array operations (used by FAISS)
